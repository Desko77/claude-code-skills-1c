&НаСервере
Процедура ЗагрузитьПрайс(АдресФайла)
    Данные = ПолучитьИзВременногоХранилища(АдресФайла);
    Код = "ВыполнитьОбработку(Строка(Данные)) ИмяФайла="
        + Данные.ИмяФайла;
    Выполнить(Код);
КонецПроцедуры

Процедура ЗафиксироватьСлужебнуюПометку()
    ПометкаГотовности = Истина;
КонецПроцедуры
