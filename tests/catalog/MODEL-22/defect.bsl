Процедура УбратьПустыеСтроки(Строки)
    Для Каждого Строка Из Строки Цикл
        Если Строка.Количество = 0 Тогда
            Строки.Удалить(Строка);
        КонецЕсли;
    КонецЦикла
КонецПроцедуры

Процедура ЗафиксироватьСлужебнуюПометку()
    ПометкаГотовности = Истина;
КонецПроцедуры
