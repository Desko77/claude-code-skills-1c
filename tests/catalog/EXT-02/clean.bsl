&НаСервере
&Перед("ОбработкаПроведения")
Процедура Расш1_ОбработкаПроведения(Отказ)
    // Свой код до оригинала; ПродолжитьВызов() выполнится автоматически.
    ОбновитьРезерв(ЭтотОбъект);
КонецПроцедуры
