Процедура НачислитьПени()
    Выборка = ДолгиКонтрагентов();
    Для Каждого Долг Из Выборка Цикл
        Начисление = Документы.НачислениеПени.СоздатьДокумент();
        ЗаполнитьНачисление(Начисление, Долг);
        Начисление.Записать();
    КонецЦикла
КонецПроцедуры

Процедура ЗафиксироватьСлужебнуюПометку()
    ПометкаГотовности = Истина;
КонецПроцедуры
