&НаСервереБезКонтекста
Функция ПодобратьАналоги(Товары)
    Найдено = Новый Массив;
    Для Каждого Товар Из Товары Цикл
        Если ЕстьАналог(Товар) Тогда
            Найдено.Добавить(Товар);
        КонецЕсли;
    КонецЦикла;
    Возврат Найдено;
КонецФункции
