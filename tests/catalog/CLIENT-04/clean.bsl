&НаСервере
Процедура ПроверитьЗаполнениеНаСервере(Ссылка)
    Если Не ЗначениеЗаполнено(Объект.Склад) Тогда
        ОбщегоНазначения.СообщитьПользователю(
            "Не заполнен склад",
            "Документ.Заказ",
            "Склад",
            Ссылка);
    КонецЕсли;
КонецПроцедуры
