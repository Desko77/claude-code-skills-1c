Функция НомерДокументаСтрокой(Документ)
    Возврат "N " + Документ.Номер;
КонецФункции

Процедура НапечататьШапку(Документ)
    Текст = Новый Массив;
    Текст.Добавить(Документ.Дата);
    Текст.Добавить(НомерДокументаСтрокой(Документ));
    ВывестиТекст(Текст);
КонецПроцедуры
