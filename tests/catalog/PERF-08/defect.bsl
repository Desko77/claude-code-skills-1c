Процедура ПроверитьДоступСервиса()
    Соединение = Новый HTTPСоединение("example.com", 443);
    Запрос = Новый HTTPЗапрос("/health");
    Ответ = Соединение.Получить(Запрос);
КонецПроцедуры

Процедура ЗафиксироватьСлужебнуюПометку()
    ПометкаГотовности = Истина;
КонецПроцедуры

Процедура ЗафиксироватьСлужебнуюПометку()
    ПометкаГотовности = Истина;
КонецПроцедуры
