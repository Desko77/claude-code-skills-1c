Процедура ПриЗаписи(Отказ)
    НачатьТранзакцию();
    Попытка
        ЗаписатьДопРеквизиты(ЭтотОбъект);
        ЗафиксироватьТранзакцию();
    Исключение
        ОтменитьТранзакцию();
        ВызватьИсключение;
    КонецПопытки;
КонецПроцедуры
