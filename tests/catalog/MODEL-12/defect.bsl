Процедура ОбработатьОтвет(Ответ)
    Если ПолучитьДанныеОтвета(Ответ).Код = 0 Тогда
        ОтправитьУведомление(
            ПолучитьДанныеОтвета(Ответ).Номер);
        ЗаписатьЖурнал(
            ПолучитьДанныеОтвета(Ответ).ДатаОтвета);
        СохранитьНомер(
            ПолучитьДанныеОтвета(Ответ).Номер);
    КонецЕсли;
КонецПроцедуры
