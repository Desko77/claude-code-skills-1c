Процедура ПересчитатьИтоги(Строки, Валюта, Дата)
    Для Каждого Строка Из Строки Цикл
        Курс = ПолучитьКурсВалюты(Валюта, Дата);
        Строка.СуммаВРублях = Строка.Сумма * Курс;
    КонецЦикла;
КонецПроцедуры
