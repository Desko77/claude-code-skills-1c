&НаКлиенте
Процедура ПроверитьСтроки(Команда)
    Для Каждого Строка Из Объект.Товары Цикл
        СтрокаГодна = ПроверитьСтрокуНаСервере(Строка.Номенклатура);
        Если НЕ СтрокаГодна Тогда
            ПометитьСтроку(Строка);
        КонецЕсли;
    КонецЦикла;
КонецПроцедуры
