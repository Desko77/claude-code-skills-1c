---
paths:
  - "**/*.bsl"
  - "**/*.os"
---

# 1C Code Review Checklist

Применяется: при ревью кода 1С (*.bsl, *.os).

---

## Классификация находок

### Critical - блокировать коммит

- Синтаксические ошибки
- SQL injection (конкатенация строк в запросе вместо параметров)
- Риск порчи данных
- Пробои безопасности (hardcoded пароли, обход RLS)
- Операции >10с без оптимизации
- Нарушения БСП с breaking changes

### High - исправить до merge

- Логические ошибки
- Отсутствие обработки ошибок (пустой Исключение)
- N+1 запросы (запрос в цикле)
- Отсутствие индексов на фильтруемых полях
- Недостаточный контроль доступа (нет проверки прав)
- Дублирование кода >50 строк
- Цикломатическая сложность >15 (количество независимых путей выполнения в функции)

### Medium - исправить в спринте

- Нарушение конвенций именования
- Отсутствие документации экспортных функций
- Субоптимальные алгоритмы
- Code smells (God-модуль, длинные функции >200 строк)
- Незначительные отклонения от БСП
- Проблемы с тестируемостью

### Low - технический долг

- Форматирование
- Стиль комментариев
- Именование переменных
- Мелкие оптимизации
- Возможности для рефакторинга

---

<!-- catalog:begin -->

## Каталог дефектов

Секция генерируется `tools/gen_catalog_index.py` по карточкам каталога скила
`1c-code-review`; между маркерами не правится. Перевод важности карточек в действия
ревью - `skills/1c-code-review/references/severity.md`.

| Группа | Идентификатор | Важность | Триггер |
|--------|---------------|----------|---------|
| CLIENT | CLIENT-01 | Major | Из клиентской процедуры подряд вызывается несколько серверных функций для одной операции |
| CLIENT | CLIENT-02 | Major | Серверная функция модуля формы с директивой НаСервере не обращается к реквизитам формы |
| CLIENT | CLIENT-03 | Major | В цикле клиентской процедуры вызывается серверная функция |
| CLIENT | CLIENT-04 | Major | Для сообщения пользователю вызывается Сообщить() вместо ОбщегоНазначения.СообщитьПользователю |
| EXT | EXT-01 | Major | Тело заимствованной процедуры расширения изменено напрямую, без перехватчика |
| EXT | EXT-02 | Critical | Перехватчик &Вместо не вызывает ПродолжитьВызов(), и полная замена типового метода не заявлена в шапке как намеренная |
| EXT | EXT-03 | Minor | Процедура расширения названа без префикса расширения |
| EXT | EXT-04 | Major | В Rights.xml роли расширения есть блок прав вида Enum.ИмяПеречисления |
| FORM | FORM-01 | Critical | Локальная переменная объявлена с именем реквизита формы и получает значение |
| META | META-01 | Major | У двух объектов метаданных в исходниках один и тот же uuid |
| MODEL | MODEL-01 | Minor | Более четырех уровней вложенности условий и циклов в одной процедуре |
| MODEL | MODEL-02 | Critical | Набор записей с замещающей записью Записать(Истина) получает отбор не по всем ключевым измерениям регистра |
| MODEL | MODEL-03 | Major | Виды характеристик создаются программно или регистры подсистемы свойств пишутся напрямую, минуя ее программный интерфейс |
| MODEL | MODEL-04 | Major | Отказ операции возвращается выходным параметром-флагом или структурой результата, а вызывающий код продолжает работу без его проверки |
| MODEL | MODEL-05 | Minor | Параметр-коллекция (структура, массив, таблица значений) объявлен без Знач, хотя метод ее не изменяет |
| MODEL | MODEL-06 | Minor | Набор функций-помощников, каждая читает из одного и того же ответа одно поле и приводит его к нужному типу |
| MODEL | MODEL-07 | Major | Ячейка табличного документа читается методом Получить(), которого у типа ТабличныйДокумент нет |
| MODEL | MODEL-08 | Major | У объекта ПараметрыЗаписиJSON, созданного без аргументов, значение задается присваиванием свойству |
| MODEL | MODEL-09 | Minor | Выборка запроса в цикле собирается в массив структур с теми же полями, что и список полей запроса |
| MODEL | MODEL-10 | Major | Ключ соответствия строится склейкой нескольких значений в строку через разделитель |
| MODEL | MODEL-11 | Minor | Обращение к реквизиту собственной конфигурации обложено проверкой его наличия через метаданные |
| MODEL | MODEL-12 | Minor | Результат вызова своей функции читается через точку в нескольких местах метода вместо сохранения в переменную |
| MODEL | MODEL-13 | Minor | Во всех точках вызова метод получает одно и то же значение параметра, а умолчания в сигнатуре нет |
| MODEL | MODEL-14 | Major | Цепочка Если-ИначеЕсли из трех и более веток по значению или типу не имеет ветки Иначе |
| MODEL | MODEL-15 | Major | Полю ссылочного типа присваивается примитив: пустая строка, ноль или булево |
| MODEL | MODEL-16 | Critical | Правка удаляет метод модуля, а вызовы его из других процедур того же модуля остаются |
| PERF | PERF-01 | Critical | В цикле создается Новый Запрос или выполняется Выполнить() на каждой итерации |
| PERF | PERF-02 | Critical | У ссылочной переменной подряд читается несколько реквизитов через точку |
| PERF | PERF-03 | Minor | В цикле вызывается функция с одинаковыми аргументами, результат не сохраняется |
| PERF | PERF-04 | Minor | Два вложенных цикла ищут совпадение ключей между коллекциями |
| PERF | PERF-05 | Minor | Внутри цикла читается константа или разыменовывается предопределенный элемент, хотя значение не зависит от итерации |
| PERF | PERF-06 | Minor | НайтиПоНаименованию или НайтиПоКоду ищет элемент, от которого зависит работа механизма |
| PROC | PROC-01 | Major | В отчете заявлено выполнение проверки (проверено, работает, исправлено) без команды прогона и его результата |
| PROC | PROC-02 | Major | Вывод "код компилируется" сделан по загрузке или выгрузке конфигурации, валидатору XML или анализатору, который модули не компилирует |
| PROC | PROC-03 | Major | Правка удаляет из исходного кода проверки редких и граничных случаев, а добавленные операции записи выполняются без чтения результата |
| QUERY | QUERY-01 | Critical | В списке полей SELECT стоит вложенный ВЫБРАТЬ в скобках |
| QUERY | QUERY-02 | Major | Условие на измерение виртуальной таблицы стоит в ГДЕ, а не в параметрах таблицы |
| QUERY | QUERY-03 | Major | Запрос без ПЕРВЫЕ читает таблицу целиком, когда обработка использует часть строк |
| QUERY | QUERY-04 | Major | Через точку читается поле составного типа без ВЫРАЗИТЬ |
| QUERY | QUERY-05 | Minor | Из ссылки в списке полей читается Наименование только ради отображения |
| QUERY | QUERY-06 | Major | В разделе ИЗ соединение выполняется с вложенным ВЫБРАТЬ в скобках |
| QUERY | QUERY-07 | Major | В разделе ИЗ соединение выполняется напрямую с виртуальной таблицей |
| QUERY | QUERY-08 | Major | Условия одной таблицы в ГДЕ соединены через ИЛИ по разным полям |
| QUERY | QUERY-09 | Minor | Пакеты объединены через ОБЪЕДИНИТЬ без слова ВСЕ при отсутствии дубликатов |
| QUERY | QUERY-10 | Major | Условия ГДЕ заданы по полям, пропускающим первые поля индекса таблицы |
| QUERY | QUERY-11 | Minor | Ограничение выборки задано отбором настроек СКД, текст запроса набора без условия |
| QUERY | QUERY-12 | Minor | Из полей правой таблицы ЛЕВОГО СОЕДИНЕНИЯ значения читаются без ЕСТЬNULL |
| QUERY | QUERY-13 | Major | Таблица значений со строковой колонкой без КвалификаторыСтроки передается параметром в запрос и соединяется с таблицей базы |
| QUERY | QUERY-14 | Critical | В разделе ГДЕ стоит вложенный ВЫБРАТЬ, ссылающийся на поле внешнего запроса |
| QUERY | QUERY-15 | Major | Временная таблица помещена без ИНДЕКСИРОВАТЬ ПО и в том же пакете участвует в соединении по этим полям |
| QUERY | QUERY-16 | Critical | Псевдоним поля или таблицы в запросе совпадает со служебным словом языка запросов |
| QUERY | QUERY-17 | Major | Псевдоним источника в запросе совпадает с именем колонки или табличной части, видимыми в том же запросе |
| QUERY | QUERY-18 | Minor | В запросе стоит ПЕРВЫЕ N при N больше единицы, а секции УПОРЯДОЧИТЬ ПО нет |
| SEC | SEC-01 | Critical | Значение подставляется в текст запроса сложением строк вместо параметра |
| SEC | SEC-02 | Critical | УстановитьПривилегированныйРежим(Истина) или метод привилегированного общего модуля читает либо изменяет данные, состав которых задан параметрами от пользователя, и результат уходит пользователю или изменение делается от его имени - без явной проверки права до операции |
| SEC | SEC-03 | Critical | В коде встречается пароль, токен или ключ строковым литералом |
| SEC | SEC-04 | Major | Данные из файла, HTTP-сервиса или другой базы используются без проверки структуры и типов |
| SEC | SEC-05 | Major | Строка из внешних данных конкатенацией собирается в HTML-документ или поле HTML формы |
| TXN | TXN-01 | Major | НачатьТранзакцию() вызывается внутри блока Попытка |
| TXN | TXN-02 | Major | Между НачатьТранзакцию() и Попытка стоят операторы |
| TXN | TXN-03 | Major | После ЗафиксироватьТранзакцию() внутри Попытка стоят еще операторы |
| TXN | TXN-04 | Major | В блоке Исключение до ОтменитьТранзакцию() стоят другие операторы |
| TXN | TXN-05 | Major | Блок Исключение кода с транзакцией завершается без ВызватьИсключение |
| TXN | TXN-06 | Major | Внутри ПередЗаписью, ПриЗаписи или ОбработкаПроведения вызывается НачатьТранзакцию() |
| TXN | TXN-07 | Major | БлокировкаДанных устанавливается после чтения данных, по которым принимается решение |
| TXN | TXN-08 | Minor | Блокировка устанавливается без значений отбора или по всей таблице |
| TXN | TXN-09 | Major | Две операции захватывают одни и те же ресурсы в разном порядке |
| TXN | TXN-10 | Major | Между НачатьТранзакцию() и ЗафиксироватьТранзакцию() выполняются диалоги, обращения к внешним системам или запись в файлы |
| TXN | TXN-11 | Major | Запись документа и его проведение объединены в одну явную транзакцию |

<!-- catalog:end -->

## Шкала уверенности

| Баллы | Уровень | Описание |
|-------|---------|----------|
| 0-25 | Низкая | Возможно false positive |
| 26-50 | Средняя | Стоит обсудить |
| 51-75 | Высокая | Вероятная реальная проблема |
| 76-100 | Очень высокая | Подтвержденная проблема с доказательствами |

Репортить: при ревью кода - только >=75, при ревью архитектуры - >=50.

**Фильтр уверенности** - цель: отсечь ложные срабатывания:
- Уверенность подтверждается конкретикой: сценарий сбоя (какие данные/действия ломают код) и подтверждение кодом (файл:строка). Без них оценка не выше средней
- Находки ниже порога - отбросить или вынести отдельным блоком "Гипотезы (не подтверждены)", не смешивая с основными
- В отчете находки группировать по severity (Critical -> Low)

---

## Security-паттерны 1С

### Уязвимости - искать

- **SQL injection**: конкатенация строк в `Запрос.Текст` вместо `&Параметр`
- **XSS**: неэкранированный вывод в формах
- **Обход RLS**: отсутствие проверок `ПравоДоступа()` / `РольДоступна()`
- **Hardcoded секреты**: пароли, токены, ключи в коде
- **Небезопасная обработка данных**: внешние данные без валидации

### Best practices

- Параметризованные запросы (`&Параметр`, не конкатенация)
- Валидация всех внешних входов
- Проверка прав перед операциями: `ПравоДоступа("Изменение", Метаданные.Справочники.Клиенты)`
- Секреты - в безопасном хранилище, не в коде

---

## Формат отчета ревью

```
Файл: путь/к/модулю.bsl
Статус: PASS | FAIL | NEEDS_WORK
Critical: N, High: N, Medium: N, Low: N

[SEVERITY] Строка XX: Краткое описание
  Проблема: что не так
  Код: фрагмент проблемного кода
  Исправление: как исправить
  Пример: правильный код
  Влияние: на что влияет
```

---

## Автоматические проверки через MCP

Обязательные MCP-проверки при ревью - см. `1c-coding-standards.md` секция "Обязательные автоматические проверки" (шаги, throttling, fallback).

**Fallback без MCP:** ручная проверка по `anti_patterns.md` + секциям Critical/High этого чек-листа.
